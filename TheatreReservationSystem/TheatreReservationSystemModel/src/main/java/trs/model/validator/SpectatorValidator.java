package trs.model.validator;

import trs.model.Spectator;

public class SpectatorValidator implements IValidator<Spectator> {

    @Override
    public void validate(Spectator spectator) throws ValidatorException {
    }
}
