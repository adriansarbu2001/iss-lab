package trs.model.validator;

import trs.model.Reservation;

public class ReservationValidator implements IValidator<Reservation> {

    @Override
    public void validate(Reservation reservation) throws ValidatorException {
    }
}
